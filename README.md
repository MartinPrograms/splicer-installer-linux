# Splice Installer Linux
This is a Splice Installer for Linux.  
The default Splice executable from splice.com does not function through Wine, this installer downloads the Splice binaries directly, and then installs it to ./splice by default.

To use it just run `./splice-install.sh` and it will download the latest Splice release, extract it, and place it in `./splice`.
